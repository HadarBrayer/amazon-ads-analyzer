# For Streamlit Community Cloud deployment only (streamlit_app.py).
# Poetry (pyproject.toml / poetry.lock) remains the source of truth for
# local development — keep these versions in sync with it by hand, since
# Streamlit Cloud installs from this file, not from Poetry.
streamlit==1.64.0
pandas==3.0.6
altair==6.3.0
pydantic==2.13.5
